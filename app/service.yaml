apiVersion: v1
kind: Service
metadata:
  name: hello-service
spec:
  type: NodePort
  selector:
    app: hello-app
  ports:
    - port: 80            # порт сервиса внутри кластера
      targetPort: 32777   # порт приложения в контейнере
      nodePort: 30080     # порт снаружи, на ноде minikube
